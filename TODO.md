Construa a página de configurações

Ao logar usando o google você deve usar o avatar(se disponivel) da conta do google

Transforme a pasta files/ em ansible/ crie um playbook simples mais completo usando o ansible com:
- Suporte as units systemd do go
- Proxy reverso para o vue e go usando o nginx
- Instalação de certificados letsencrypt(caso eles não existam, cuidado com o block por parte do cloudflare/letsencrypt)
- Instalação do go, nginx, bun, node(se ainda não estiverem instalados), mas fique atento o bun na vps é instalado na home do usuário nginx
- Criação dos usuários e grupos caso ainda não existam(nginx por exemplo)

Crie uma forma de expostar um relatório dos gastos em: 1. ODS, 2. XLSX, 3. PDF, 4. JSON com os gráficos e permita tambem importar estes arquivos(como uma forma de backup)

Melhore a tabela das transações, em desktops ela deve ser um pouco mais larga, porem ao mesmo tempo mais compacta, ela tambem precisa ser responsiva.

Ao editar um ítem crie um modal bonito, elegante e responsivo usando classes do tailwindcss

