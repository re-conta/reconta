server {
    listen 443 ssl;
    server_name reconta.local;

    ssl_certificate     /etc/nginx/certs/reconta.local.pem;
    ssl_certificate_key /etc/nginx/certs/reconta.local-key.pem;

    root /usr/share/nginx/html;
    index index.html;

    # Go API — proxy reverso para o backend (mesmo pod, via localhost)
    location /api/ {
        proxy_pass http://127.0.0.1:3020;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Vue.js SPA — serve arquivos estáticos e fallback para index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
