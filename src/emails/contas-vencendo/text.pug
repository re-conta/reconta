| Olá, #{name}!
|
| CONTAS FIXAS — RESUMO
|
if overdueBills.length > 0
  | VENCIDAS:
  each bill in overdueBills
    | • #{bill.name} — #{bill.amountFormatted} (venceu no dia #{bill.dueDay}, #{bill.daysOverdue} dia(s) em atraso)
  |
if upcomingBills.length > 0
  | A VENCER:
  each bill in upcomingBills
    | • #{bill.name} — #{bill.amountFormatted} (vence no dia #{bill.dueDay}, em #{bill.daysUntil} dia(s))
  |
| Acesse o ReConta para registrar os pagamentos:
| #{appUrl}
|
| Para gerenciar notificações: #{settingsUrl}
